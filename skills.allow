# Skills marked `locked: true` only run if their name is listed here (one per line).
# Lines starting with # are ignored. Edit, then restart the service to apply.
# To arm the blog-post skill, uncomment the next line:
# blog-post
