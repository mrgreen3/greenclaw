Prefixes:
  cc <prompt>   force Claude Code (email, web, GitHub, full reach)
  gg <prompt>   force Gemini 2.5 Flash (free tier, tool loop)
  (no prefix)   Claude Code; falls back to local Qwen if CC errors

Commands:
  usage / calls   CC call count today
  /version        greenclaw version
  /cheat          this list
  /watch          scheduled jobs
  /memory         memory vault stats
  /inbox on|off   toggle GitHub inbox watcher
  /regreen        restart the bot

Skills:
{skills}