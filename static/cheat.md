Prefixes:
  cc <prompt>   force Claude Code (email, web, GitHub, full reach)
  gc <prompt>   force local Qwen (free, on-device)
  (no prefix)   Qwen first; escalates to CC when needed

Commands:
  usage / calls   CC call count today
  /cheat          this list

Skills:
{skills}
